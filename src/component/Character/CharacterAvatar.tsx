/*
import { makeStyles } from '@material-ui/core';
import { Character } from '../../store/combat';

export interface PropTypes {
  character: Character;
  withName?: boolean
}

const useStyles = makeStyles((theme) => ({

}));

const CharacterChip = ({ character, withName }: PropTypes) => {
  return null;
};

export default CharacterChip;
*/

export {};
